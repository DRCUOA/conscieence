{
  "name": "conscieence",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --env development",
    "preview": "wrangler dev --env development --local",
    "deploy": "wrangler deploy --env production",
    "deploy:dev": "wrangler deploy --env development"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
